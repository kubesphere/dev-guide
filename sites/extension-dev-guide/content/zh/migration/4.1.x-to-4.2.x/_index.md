---
title: 从 4.1.x 升级到 4.2.x
weight: 01
description: 如何从 4.1.x 升级到 4.2.x 版本
---

### 4.2.0

#### 前端

由于 4.2.0 采用了全新设计的 UI，因此之前的挂载位置有所变化。4.2.0 不再支持 `topbar` ，请使用 `global`，并在页面的「组件坞」中「自定义组件入口」，以还原至之前 `topbar` 的效果。
